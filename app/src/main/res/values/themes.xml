<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.VovasApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorOnPrimary">@color/white</item>
        <item name="color">@color/white</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="cardBackgroundColor">@color/white</item>
        <item name="colorAccent">@color/white</item>
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="CustomAlertDialogTheme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="colorPrimary">@color/neon</item>
        <item name="colorAccent">@color/neon</item>
    </style>

    <style name="CardViewTransparentBorder" parent="Widget.MaterialComponents.CardView">
        <item name="cardBackgroundColor">@android:color/transparent</item>
        <item name="cardElevation">0dp</item>
        <item name="contentPadding">0dp</item>
    </style>

    <style name="Theme.VovasApp" parent="Base.Theme.VovasApp" />
</resources>